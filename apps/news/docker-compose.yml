services:
  ai-news-hub:
    build: .
    container_name: ai-news-hub
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/news.db
      PORT: 8000
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
